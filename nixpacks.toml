[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd math-grade5-practice && npm install"]

[phases.build]
cmds = ["cd math-grade5-practice && npm run build"]

[start]
cmd = "cd math-grade5-practice && npm run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
